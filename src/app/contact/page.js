import ContactContent from "./ContactContent";

export const metadata = {
  title: "Contact Tooba Ameer — Design & Development Manager",
  description: "Get in touch with Tooba Ameer. Reach out via email, view location and availability, or submit a message through the contact form.",
};

export default function ContactPage() {
  return <ContactContent />;
}
